function leap() {
    local LEAP
    LEAP="$GOPATH/bin/leap"
 
    if [ $# -lt 1 ]; then
	echo "1"
	$LEAP
    elif [ $1 = "add" ] || [ $1 = "rm" ] || [ $1 = "list" ] || [ $1 = "help" ]; then
	echo "2"
	$LEAP $@
    else
	echo "3"
        cd $($LEAP "$@")
    fi
}
